apiVersion: v1
kind: ConfigMap
metadata:
  name: manager-menu-service-configmap
data:
  rabbitmq_host: "rabbitmq-service"
  cn_db: "Server=sqlserver-service;Database=ManagerMenuDb;User Id=sa;Password=YourStrong!Password;TrustServerCertificate=True"

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: zabbix-agent-config
data:
  zabbix_agentd.conf: |
    Server=0.0.0.0/0
    ServerActive=zabbix-zabbix-server
    Hostname=manager-menu-service-container
    ListenPort=10050
    ListenIP=0.0.0.0
    LogType=console

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: manager-menu-service-deployment
  labels:
    app: manager-menu-service
  annotations:
    type: "consumer"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: manager-menu-service
  template:
    metadata:
      labels:
        app: manager-menu-service
    spec:
      containers:
      - name: manager-menu-service
        image: mauropy01/fast-tech-products-manager-service:latest
        ports:
        - containerPort: 8080
        env:
        - name: RABBITMQ_HOST
          valueFrom:
            configMapKeyRef:
              name:  manager-menu-service-configmap
              key: rabbitmq_host
        - name: CONNECTION_DATABASE
          valueFrom:
            configMapKeyRef:
              name:  manager-menu-service-configmap
              key: cn_db
      - name: zabbix-agent
        image: zabbix/zabbix-agent:alpine-6.0-latest
        volumeMounts:
        - name: zabbix-agent-config
          mountPath: /etc/zabbix/zabbix_agentd.conf
          subPath: zabbix_agentd.conf
        ports:
        - containerPort: 10050

      volumes: 
      - name: zabbix-agent-config
        configMap:
          name: zabbix-agent-config
---

apiVersion: v1
kind: Service
metadata:
  name: manager-menu-service-service
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: manager-menu-service


---
apiVersion: v1
kind: Service
metadata:
  name: manager-menu-service-zabbix-agent-service
spec:
  selector:
    app: manager-menu-service
  ports:
  - protocol: TCP
    port: 10050
    targetPort: 10050
  type: ClusterIP
